# -- Object Detection (YOLO) --------------------------------------------------
ultralytics>=8.3.0

# -- Image I/O -----------------------------------------------------------------
opencv-python-headless>=4.8.0

# Base image torch==2.1.2 build theo ABI numpy 1.x — numpy 2.x làm gãy
# torch.from_numpy() ("Numpy is not available"). Ghim <2 tới khi đổi base torch.
numpy<2

# -- Utilities -----------------------------------------------------------------
tqdm>=4.66.0
pyyaml>=6.0
